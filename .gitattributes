# Keep source checkouts deterministic across Windows and CI.
* text=auto eol=lf
*.sln text eol=crlf
*.ico binary
*.png binary
*.woff2 binary

# These files are copied byte-for-byte from the pinned upstream MD4C release.
third_party/md4c/** -whitespace
